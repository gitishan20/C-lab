#include <stdio.h>
int main()
{
    int number, i_84;
    printf("Enter a positive integer:");
    scanf("%d", &number);
    printf("Factors of %d are:", number);
    for (i_84 = 1; i_84 <= number; ++i_84)
    {
        if (number % i_84 == 0)
            printf
            {
                printf("%d", i_84);
            }
    }
    return 0;
}
